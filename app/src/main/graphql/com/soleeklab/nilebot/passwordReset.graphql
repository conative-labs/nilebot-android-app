mutation passwordReset(
    $email: String,
    $phone:iPhoneNumber,
    $code: String!,
    $password: String!

) {

    passwordReset(
        email: $email,
        phone:$phone,
        code: $code
        password: $password
    )
}